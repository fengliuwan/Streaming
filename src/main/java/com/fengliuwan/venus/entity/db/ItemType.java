package com.fengliuwan.venus.entity.db;

public enum ItemType {
    STREAM, VIDEO, CLIP

    //MONDAY, TUESDAY

    // setSchedule(MONDAY)  --> can only has the CONSTANTS in enum
    // setSchedule(11)
}
